<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/welcome_background">

    <!-- Botón ajustado: Eliminados textos duplicados. 
         Posicionado con márgenes para que quede justo debajo del texto de la imagen de fondo.
         Tamaño reducido para que sea elegante. -->
    <ImageButton
        android:id="@+id/btnComenzar"
        android:layout_width="160dp"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:layout_marginEnd="35dp"
        android:layout_marginBottom="-16dp"
        android:background="@android:color/transparent"
        android:src="@drawable/btn_comenzar_new"
        android:scaleType="fitCenter"
        android:adjustViewBounds="true"
        android:alpha="0"
        android:contentDescription="Comenzar" />

</RelativeLayout>
